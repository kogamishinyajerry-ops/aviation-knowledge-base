---
phase: 05-rag-pipeline-design-document-only-no-run
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02", "05-03"]
files_modified:
  - .planning/design/RAG_PIPELINE.md
  - .planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md
autonomous: true
requirements: [RAG-01, RAG-02, RAG-03, RAG-04, RAG-05, RAG-06, RAG-07, RAG-08]
must_haves:
  truths:
    - "RAG_PIPELINE.md is amended with a final §11 'REQ-Coverage Matrix' that maps each of RAG-01..08 to the artifact + section that delivers it (forward traceability)"
    - "RAG_PIPELINE.md AI 接力开发指南 §Locked-vs-Directional table is reviewed and updated to reflect what plans 05-01..03 actually delivered (no aspirational locks)"
    - ".planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md is a one-page reverse-traceability matrix showing every Phase-5 artifact and its REQ-IDs (mirror Phase 4's 04-VERIFICATION.md style if present, else stand alone)"
    - "All 6 ROADMAP success criteria for Phase 5 pass automated grep checks (verifier reproduces them)"
    - "5-minute stranger test sample: pick RAG_PIPELINE.md, evaluation/queries.yaml, scripts/exporters/to_ragflow.py — a fresh AI session reading these in order can describe (a) the chunking strategy, (b) the citation injection mechanism, (c) the eval contract, (d) the RAGFlow exporter contract — without consulting any other file (acceptance: written check, not measured)"
    - "No new dependencies added across all 4 plans (verifier greps requirements*.txt + pyproject.toml — no diffs since Phase 4 close)"
    - "scripts/exporters/to_ragflow.py still has --help working AND raises NotImplementedError on real-work paths (regression check that 05-04 didn't accidentally break 05-03)"
  artifacts:
    - path: .planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md
      provides: "Phase-5 REQ-coverage matrix (8 REQs × artifact × verifier command)"
      contains: "RAG-01"
      min_lines: 50
    - path: .planning/design/RAG_PIPELINE.md
      provides: "Augmented with §11 REQ-Coverage Matrix"
      contains: "## 11. REQ-Coverage Matrix"
  key_links:
    - from: ".planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md"
      to: "RAG_PIPELINE.md, evaluation/queries.yaml, scripts/exporters/to_ragflow.py"
      via: "explicit Artifact column in the matrix"
      pattern: "RAG_PIPELINE\\.md|queries\\.yaml|to_ragflow\\.py"
    - from: "RAG_PIPELINE.md §11"
      to: ".planning/REQUIREMENTS.md §RAG-01..08"
      via: "direct REQ-ID references in the table"
      pattern: "RAG-0[1-8]"
---

<objective>
Close Phase 5 with cross-doc consistency: (1) append §11 REQ-Coverage Matrix to RAG_PIPELINE.md (forward traceability — for each REQ, which section delivers it), (2) refresh the AI 接力开发指南 Locked-vs-Directional table to match what plans 05-01..03 actually shipped (not what we hoped to ship), (3) write `05-COVERAGE.md` as the phase-level reverse-traceability matrix that the orchestrator's verification step can grep against.

Purpose: Phase 5 has 4 artifacts (RAG_PIPELINE.md, evaluation/queries.yaml, evaluation/README.md, scripts/exporters/to_ragflow.py). The Phase-5 verifier needs a single index page to confirm all 8 REQs are covered. The 5-minute stranger test (AIH-02) needs a doc that orients a fresh AI to "where does X live in Phase 5?" without re-reading every artifact. This plan delivers both.

Output: Modified `RAG_PIPELINE.md` (append §11, refresh AI 接力 table) + new `05-COVERAGE.md` (phase index).

Note on dependencies: This plan depends on 05-01, 05-02, 05-03 (Wave 1 plans). It runs in Wave 2 because it READS the Wave 1 outputs to build the matrix; it does NOT recreate them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/design/RAG_PIPELINE.md
@evaluation/queries.yaml
@evaluation/README.md
@scripts/exporters/to_ragflow.py
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/04-demo-data-document-import-spec/04-VALIDATION.md
@.planning/phases/05-rag-pipeline-design-document-only-no-run/05-01-SUMMARY.md
@.planning/phases/05-rag-pipeline-design-document-only-no-run/05-02-SUMMARY.md
@.planning/phases/05-rag-pipeline-design-document-only-no-run/05-03-SUMMARY.md

<interfaces>
Phase 5 REQ-IDs (verbatim from REQUIREMENTS.md):
- RAG-01: chunking with table preservation (RAGFlow OpenDataLoader-PDF)
- RAG-02: BGE-M3 + bge-reranker-v2-m3 default + mini-benchmark plan
- RAG-03: hybrid retrieval (vector + BM25 + RRF) + query expansion
- RAG-04: citation injection — system-side `[CITE:chunk_id]` token, NOT LLM-self-cite
- RAG-05: guardrail — empty/all-below-threshold → "not found" without LLM
- RAG-06: cross-lingual — bilingual glossary + entity i18n at index
- RAG-07: evaluation/queries.yaml ≥30 queries with expected source docs
- RAG-08: scripts/exporters/to_ragflow.py skeleton + design spec

ROADMAP Phase 5 success criteria 1..6 (verbatim — these are what 05-VERIFICATION will grep):
1. RAG_PIPELINE.md documents chunking with explicit table-as-atomic preservation, citing RAGFlow 0.25.1 OpenDataLoader-PDF
2. Embedding selection: BGE-M3 + bge-reranker-v2-m3 default + reproducible mini-benchmark plan vs nomic-embed-text + multilingual-e5-large; cross-lingual ZH/EN per AeroPower-RAG findings
3. Citation injection spec: system-side `[CITE:chunk_id]` token; render layer resolves; post-validator rejects unresolved
4. Guardrail spec: hard-coded `retrieved_chunks=[] OR all-below-threshold → "not found" WITHOUT LLM call`; out-of-scope eval verifies
5. evaluation/queries.yaml ≥30 queries; ≥20% table; out-of-scope category included
6. scripts/exporters/to_ragflow.py skeleton + spec covering Git-watch → RAGFlow HTTP API + idempotent re-upload via content hash + --rebuild

Cross-cutting: AIH-01 (every design doc has AI 接力开发指南).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append §11 REQ-Coverage Matrix to RAG_PIPELINE.md</name>
  <files>.planning/design/RAG_PIPELINE.md</files>
  <read_first>
    - .planning/design/RAG_PIPELINE.md (read full current state — verify §1..§10 present)
    - .planning/REQUIREMENTS.md §"RAG Pipeline Design" (RAG-01..08 verbatim definitions)
    - evaluation/queries.yaml (count records by type — used in matrix)
    - scripts/exporters/to_ragflow.py (verify locked CLI + functions — used in matrix)
  </read_first>
  <action>
Append a new section `## 11. REQ-Coverage Matrix` AFTER §10 References. Use this exact table (fill in the Verifier-Command column with shell commands the orchestrator can run):

```markdown
## 11. REQ-Coverage Matrix

This matrix is the forward-traceability index for Phase 5. For each requirement,
the table shows the section in this document that delivers the design AND a shell
command the Phase-5 verifier can run to confirm presence.

| REQ-ID  | Delivered by                                                       | Section / File                                       | Verifier command                                                              |
|---------|--------------------------------------------------------------------|------------------------------------------------------|-------------------------------------------------------------------------------|
| RAG-01  | Chunking strategy with table-atomic + §-clause-atomic rules        | RAG_PIPELINE.md §2                                   | `grep -q "OpenDataLoader-PDF" .planning/design/RAG_PIPELINE.md && grep -q "atomic" .planning/design/RAG_PIPELINE.md`              |
| RAG-02  | BGE-M3 default + bge-reranker-v2-m3 + mini-benchmark plan          | RAG_PIPELINE.md §3                                   | `grep -q "BGE-M3" .planning/design/RAG_PIPELINE.md && grep -q "bge-reranker-v2-m3" .planning/design/RAG_PIPELINE.md && grep -qE "nomic-embed-text\|multilingual-e5-large" .planning/design/RAG_PIPELINE.md` |
| RAG-03  | Hybrid retrieval vector + BM25 + RRF + query expansion w=0.3        | RAG_PIPELINE.md §4                                   | `grep -qE "RRF\|rrf" .planning/design/RAG_PIPELINE.md && grep -q "weight: 0.3" .planning/design/RAG_PIPELINE.md`                   |
| RAG-04  | Citation injection (token + render + validator)                     | RAG_PIPELINE.md §5                                   | `grep -q "\\[CITE:c_" .planning/design/RAG_PIPELINE.md && grep -q "validate_answer_citations" .planning/design/RAG_PIPELINE.md`     |
| RAG-05  | Guardrail short-circuit (no-LLM-call when below threshold)          | RAG_PIPELINE.md §6                                   | `grep -q "min_chunk_score" .planning/design/RAG_PIPELINE.md && grep -q "llm_called=False" .planning/design/RAG_PIPELINE.md`         |
| RAG-06  | Cross-lingual: BGE-M3 multilingual + glossary expansion + i18n      | RAG_PIPELINE.md §7                                   | `grep -q "i18n" .planning/design/RAG_PIPELINE.md && grep -qE "glossary\|GLOSSARY" .planning/design/RAG_PIPELINE.md`                 |
| RAG-07  | Eval set with ≥30 queries, ≥20% table, ≥3 out-of-scope             | evaluation/queries.yaml + evaluation/README.md       | `python -c "import yaml; q=yaml.safe_load(open('evaluation/queries.yaml'))['queries']; assert len(q)>=30 and sum(1 for x in q if x['type']=='table')>=6 and sum(1 for x in q if x['type']=='out_of_scope')>=3"` |
| RAG-08  | to_ragflow.py skeleton: argparse + Git-watch + idempotency + --rebuild | scripts/exporters/to_ragflow.py                  | `python scripts/exporters/to_ragflow.py --help > /dev/null && grep -q "compute_doc_id" scripts/exporters/to_ragflow.py && grep -q "rebuild_index" scripts/exporters/to_ragflow.py` |

### ROADMAP success-criterion mapping

| ROADMAP SC# | Description (verbatim from ROADMAP.md Phase 5)                                                       | REQ-ID(s)         |
|-------------|------------------------------------------------------------------------------------------------------|-------------------|
| SC-1        | RAG_PIPELINE.md documents chunking with table preservation, citing RAGFlow 0.25.1                    | RAG-01            |
| SC-2        | Embedding rationale + mini-benchmark plan + cross-lingual                                            | RAG-02 + RAG-06   |
| SC-3        | Citation injection: system-side token, render layer resolves, post-validator rejects unresolved      | RAG-04            |
| SC-4        | Guardrail hard-codes empty/below-threshold → "not found" without LLM                                 | RAG-05            |
| SC-5        | evaluation/queries.yaml ≥30, ≥20% table, out-of-scope                                                | RAG-07            |
| SC-6        | to_ragflow.py skeleton: Git-watch, content-hash idempotency, --rebuild                              | RAG-08            |
```

Constraints:
- Append cleanly AFTER existing §10 (do NOT re-write §1..§10).
- Every verifier command must be a real one-line shell command (verifier will copy-paste and run).
- After this task, RAG_PIPELINE.md ≥ 500 lines.
  </action>
  <verify>
    <automated>grep -q "## 11. REQ-Coverage Matrix" .planning/design/RAG_PIPELINE.md && for r in RAG-01 RAG-02 RAG-03 RAG-04 RAG-05 RAG-06 RAG-07 RAG-08; do grep -q "$r" .planning/design/RAG_PIPELINE.md || { echo "MISSING $r in matrix"; exit 1; }; done && grep -q "ROADMAP success-criterion" .planning/design/RAG_PIPELINE.md && grep -q "SC-1" .planning/design/RAG_PIPELINE.md && grep -q "SC-6" .planning/design/RAG_PIPELINE.md && [ "$(wc -l < .planning/design/RAG_PIPELINE.md)" -ge 500 ]</automated>
  </verify>
  <done>RAG_PIPELINE.md §11 added; all 8 RAG REQ-IDs in matrix with verifier commands; SC-1..SC-6 mapping table present; doc ≥500 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh RAG_PIPELINE.md AI 接力开发指南 Locked-vs-Directional table to match shipped reality</name>
  <files>.planning/design/RAG_PIPELINE.md</files>
  <read_first>
    - .planning/design/RAG_PIPELINE.md AI 接力开发指南 block (current Locked-vs-Directional table)
    - .planning/phases/05-rag-pipeline-design-document-only-no-run/05-01-SUMMARY.md (what 05-01 actually shipped)
    - .planning/phases/05-rag-pipeline-design-document-only-no-run/05-02-SUMMARY.md
    - .planning/phases/05-rag-pipeline-design-document-only-no-run/05-03-SUMMARY.md
  </read_first>
  <action>
Within the AI 接力开发指南 block at the top of RAG_PIPELINE.md, locate the Locked-vs-Directional table and refresh entries based on what plans 05-01..03 actually delivered. Required columns: Item / Locked? / Notes.

Final table (replace whatever's there):

| Item | Locked? | Notes |
|------|---------|-------|
| RAGFlow 0.25.1 + OpenDataLoader-PDF | **Locked** | From STACK.md; no alternative considered in v1 |
| BGE-M3 default + bge-reranker-v2-m3 | **Locked (default)** | Mini-benchmark may swap if Phase 7 measures a clear winner per §3.3 |
| Chunk size 512 / max 1024 / overlap 64 | **Locked (defaults)** | §2.3 — atomic blocks override size cap |
| Atomic-chunk rules: tables, equations, regulation §-clauses, figure-captions | **Locked** | §2.2 — Pitfall 6 prevention; CANNOT relax |
| Hybrid retrieval (vector + BM25 + RRF, k=60) | **Locked** | §4 — AeroPower-RAG-validated |
| Synonym expansion weight 0.3 | **Locked** | §4.2, §7.2 — AeroPower-RAG-validated; Pitfall 7 |
| Citation token format `[CITE:c_<8hex>]` | **Locked** | §5.1 — Pitfall 8 prevention; CANNOT change without ADR |
| LLM forbidden to self-author citations | **Locked** | §5.1 — Pitfall 8; Core Value defender |
| Post-generation citation validator | **Locked** | §5.3 — rejects unresolved chunk_ids |
| min_chunk_score = 0.5 | **Locked** | §6.1 — Pitfall 9 prevention; tunable per Phase 7 measurement only |
| min_chunks_required = 2 | **Locked** | §6.1 — single-hit retrieval too brittle |
| Canned no-context response (ZH + EN) | **Locked** | §6.2 — text is contractual; localization changes are translation only |
| LLM-not-called on guardrail trip | **Locked** | §6.3 — pipeline branch, NOT prompt instruction |
| Cross-lingual eval ≥6 in queries.yaml | **Locked** | RAG-07; verified by 05-COVERAGE.md |
| Out-of-scope eval ≥3 in queries.yaml | **Locked** | RAG-07; verified by 05-COVERAGE.md |
| to_ragflow.py CLI surface (--rebuild, --dry-run, --since=, --paths) | **Locked** | RAG-08 plan 05-03; argparse implemented |
| to_ragflow.py compute_doc_id rule (sha256 of path+content) | **Locked** | §-of-its-docstring; idempotency contract |
| LLM choice (Ollama Qwen2.5 vs remote Claude/GPT) | **Directional** | Phase 6 deployment plan picks based on hardware reality |
| Mini-benchmark numbers | **Directional** | §3.2 specifies the protocol; Phase 7 runs and updates §3.3 |
| Glossary seed-terms count target ≥50 | **Directional** | AIH-04 / Phase 6 deliverable; this doc points at it |
| Confidence-aware retrieval filter | **Directional** | Open question §9 — schema field exists, retrieval filter not wired in v1 |

Constraints:
- Replace the existing Locked-vs-Directional table only; do not touch other parts of AI 接力开发指南.
- Every "Locked" item must reference a section number from this doc OR a sibling artifact (queries.yaml / to_ragflow.py).
- ≥21 rows (15 locked + 4 directional minimum; this list has 21 — keep all).
  </action>
  <verify>
    <automated>grep -c "**Locked**" .planning/design/RAG_PIPELINE.md | awk '{ if ($1 < 15) { print "Need ≥15 Locked rows, got " $1; exit 1 } }' && grep -c "**Directional**" .planning/design/RAG_PIPELINE.md | awk '{ if ($1 < 4) { print "Need ≥4 Directional rows, got " $1; exit 1 } }' && grep -q "Citation token format" .planning/design/RAG_PIPELINE.md && grep -q "min_chunk_score = 0.5" .planning/design/RAG_PIPELINE.md && grep -q "compute_doc_id" .planning/design/RAG_PIPELINE.md && grep -q "to_ragflow.py CLI surface" .planning/design/RAG_PIPELINE.md && python -c "import re; c=open('.planning/design/RAG_PIPELINE.md').read(); ai_block_start=c.find('AI 接力开发指南'); ai_block_end=c.find('## 1. Scope and Non-Goals'); assert ai_block_start>=0 and ai_block_end>ai_block_start, 'AI 接力 block not found above §1'; block=c[ai_block_start:ai_block_end]; assert block.count('**Locked**')>=15, 'AI 接力 block has < 15 Locked rows in its table'"</automated>
  </verify>
  <done>AI 接力开发指南 Locked-vs-Directional table refreshed with ≥21 rows (≥15 Locked + ≥4 Directional); every Locked row cites a section/sibling-file; reflects shipped reality of plans 05-01..03.</done>
</task>

<task type="auto">
  <name>Task 3: Write 05-COVERAGE.md — phase-level reverse-traceability matrix + ROADMAP SC verification</name>
  <files>.planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md</files>
  <read_first>
    - .planning/phases/04-demo-data-document-import-spec/04-VALIDATION.md (mirror Per-REQ-ID Verification Map style if compatible)
    - .planning/design/RAG_PIPELINE.md §11 REQ-Coverage Matrix (just written in Task 1 — duplicate the table cross-direction)
    - .planning/ROADMAP.md §"Phase 5: RAG Pipeline Design" success criteria 1-6
  </read_first>
  <action>
Create `.planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md` with this structure:

```markdown
---
phase: 5
slug: rag-pipeline-design-document-only-no-run
status: pending-verification
created: 2026-05-03
last_updated: 2026-05-03
---

# Phase 5 — REQ + ROADMAP Coverage Matrix

> **Purpose:** Phase-level reverse-traceability — for each REQ-ID and each
> ROADMAP success criterion, document the artifact path + the one-shot
> verifier command the Phase-5 verification step runs.
>
> **Authority:** RAG_PIPELINE.md §11 is the forward index (REQ → section).
> THIS file is the reverse index (artifact → REQ + verifier-command).
> Both must agree.

## Phase 5 Artifacts

| Artifact path                                                    | Lines (target)        | REQ-IDs delivered                  | Plan    |
|------------------------------------------------------------------|-----------------------|------------------------------------|---------|
| .planning/design/RAG_PIPELINE.md                                  | ≥500                  | RAG-01..06                         | 05-01 + 05-04 |
| evaluation/queries.yaml                                           | ≥200                  | RAG-07                             | 05-02   |
| evaluation/README.md                                              | ≥60                   | RAG-07 (cross-cut AIH-01)          | 05-02   |
| scripts/exporters/to_ragflow.py                                   | ≥230                  | RAG-08                             | 05-03   |
| .planning/phases/05-.../05-COVERAGE.md (this file)                | ≥50                   | (phase index — no REQ direct)      | 05-04   |

## Per-REQ-ID Verification Map

| REQ-ID  | Plan    | Wave | Behavior                                                                                | Verifier command                                                                                                                                              | Status   |
|---------|---------|------|-----------------------------------------------------------------------------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------|----------|
| RAG-01  | 05-01   | 1    | Chunking strategy with table-atomic + §-clause regex; cites RAGFlow 0.25.1 OpenDataLoader-PDF | `grep -q "OpenDataLoader-PDF" .planning/design/RAG_PIPELINE.md && grep -q "v0.25.1" .planning/design/RAG_PIPELINE.md && grep -q "atomic" .planning/design/RAG_PIPELINE.md` | ⬜ pending |
| RAG-02  | 05-01   | 1    | BGE-M3 + bge-reranker-v2-m3 default + mini-benchmark plan against ≥2 candidates         | `grep -q "BGE-M3" .planning/design/RAG_PIPELINE.md && grep -q "bge-reranker-v2-m3" .planning/design/RAG_PIPELINE.md && grep -q "nomic-embed-text" .planning/design/RAG_PIPELINE.md && grep -q "multilingual-e5-large" .planning/design/RAG_PIPELINE.md` | ⬜ pending |
| RAG-03  | 05-01   | 1    | Hybrid retrieval (vector + BM25 + RRF) + query expansion weight 0.3                     | `grep -qE "RRF\|rrf" .planning/design/RAG_PIPELINE.md && grep -q "weight: 0.3" .planning/design/RAG_PIPELINE.md`                                              | ⬜ pending |
| RAG-04  | 05-01   | 1    | Citation injection: `[CITE:c_<8hex>]` token + render layer + post-generation validator   | `grep -q "\\[CITE:c_" .planning/design/RAG_PIPELINE.md && grep -q "render_citations" .planning/design/RAG_PIPELINE.md && grep -q "validate_answer_citations" .planning/design/RAG_PIPELINE.md` | ⬜ pending |
| RAG-05  | 05-01   | 1    | Guardrail: empty OR below-threshold → canned no-context response WITHOUT LLM call        | `grep -q "min_chunk_score" .planning/design/RAG_PIPELINE.md && grep -q "min_chunks_required" .planning/design/RAG_PIPELINE.md && grep -q "未在知识库中找到相关内容" .planning/design/RAG_PIPELINE.md && grep -q "llm_called=False" .planning/design/RAG_PIPELINE.md` | ⬜ pending |
| RAG-06  | 05-01   | 1    | Cross-lingual: BGE-M3 multilingual + glossary expansion plan + entity i18n at index time | `grep -q "i18n" .planning/design/RAG_PIPELINE.md && grep -qE "GLOSSARY\|glossary" .planning/design/RAG_PIPELINE.md && grep -qE "Pitfall 7" .planning/design/RAG_PIPELINE.md` | ⬜ pending |
| RAG-07  | 05-02   | 1    | evaluation/queries.yaml ≥30 records, ≥6 table, ≥6 cross_lingual, ≥3 out_of_scope, all expected_documents resolve | `python -c "import yaml; q=yaml.safe_load(open('evaluation/queries.yaml'))['queries']; assert len(q)>=30; t=[x['type'] for x in q]; assert t.count('table')>=6 and t.count('cross_lingual')>=6 and t.count('out_of_scope')>=3"` | ⬜ pending |
| RAG-08  | 05-03   | 1    | to_ragflow.py: argparse --help works, real-work raises NotImplementedError, ≥230 lines, stdlib-only, ARCHITECTURE+RAG_PIPELINE+DOC-04 cited | `python scripts/exporters/to_ragflow.py --help > /dev/null && [ $(wc -l < scripts/exporters/to_ragflow.py) -ge 230 ] && ! grep -qE "^import (requests\|httpx\|yaml\|git)" scripts/exporters/to_ragflow.py && grep -q "compute_doc_id" scripts/exporters/to_ragflow.py && grep -q "rebuild_index" scripts/exporters/to_ragflow.py && grep -q "DOC-04" scripts/exporters/to_ragflow.py` | ⬜ pending |

## ROADMAP Phase 5 Success-Criterion Verification

| SC# | Description (verbatim, ROADMAP.md)                                                                                                              | Verifier command                                                                                                                                                              | Status   |
|-----|--------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|----------|
| SC-1| RAG_PIPELINE.md documents chunking with explicit table-as-atomic-chunk preservation (RAGFlow OpenDataLoader-PDF backend), citing RAGFlow 0.25.1 | (RAG-01 verifier above)                                                                                                                                                       | ⬜ pending |
| SC-2| Embedding selection rationale + mini-benchmark + cross-lingual ZH/EN per AeroPower-RAG findings                                                 | (RAG-02 + RAG-06 verifiers above)                                                                                                                                             | ⬜ pending |
| SC-3| Citation injection spec: system-side token, render layer, post-validator rejects unresolved                                                     | (RAG-04 verifier above)                                                                                                                                                       | ⬜ pending |
| SC-4| Guardrail spec: hard-coded empty/all-below → "not found" without LLM call; out-of-scope eval verifies                                          | (RAG-05 verifier above) AND `python -c "import yaml; q=yaml.safe_load(open('evaluation/queries.yaml'))['queries']; assert sum(1 for x in q if x['type']=='out_of_scope')>=3"`  | ⬜ pending |
| SC-5| evaluation/queries.yaml ≥30 queries, ≥20% table, out-of-scope                                                                                  | (RAG-07 verifier above)                                                                                                                                                       | ⬜ pending |
| SC-6| to_ragflow.py skeleton + spec covering Git-watch, content-hash idempotency, --rebuild                                                          | (RAG-08 verifier above) AND `grep -q "Idempotency" scripts/exporters/to_ragflow.py && grep -q "rebuild" scripts/exporters/to_ragflow.py && grep -qE "sha256\|SHA-256" scripts/exporters/to_ragflow.py` | ⬜ pending |

## Phase-5 Boundary Checks (Design-Only Discipline)

| Check                                                                                                  | Verifier command                                                                                       |
|--------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------|
| No new Python deps added since Phase 4 close                                                            | `git diff --name-only origin/main -- requirements*.txt pyproject.toml \| wc -l \| grep -q '^0$'` (or document any change) |
| to_ragflow.py is stdlib-only                                                                            | `! grep -qE "^import (requests\|httpx\|yaml\|git\|aiohttp)" scripts/exporters/to_ragflow.py`           |
| No real RAGFlow HTTP call attempted in skeleton                                                         | `! grep -qE "(RAGFLOW_API_BASE.*=.*['\"]http\|requests\\.\|httpx\\.)" scripts/exporters/to_ragflow.py` |
| All design docs include AI 接力开发指南 (AIH-01)                                                          | `grep -q "AI 接力开发指南" .planning/design/RAG_PIPELINE.md && grep -q "AI 接力开发指南" evaluation/README.md && grep -q "AI 接力\|AI handoff" scripts/exporters/to_ragflow.py` |

## 5-Minute Stranger Test (sample plan)

A fresh AI session (no prior context) reads, in order:
1. .planning/design/RAG_PIPELINE.md (just the AI 接力开发指南 block + §11)
2. evaluation/README.md
3. scripts/exporters/to_ragflow.py module docstring

After 5 minutes, the session should be able to answer:
- Q1: What's the chunking strategy for a regulation PDF? → Expected: "OpenDataLoader-PDF, atomic per §-clause regex, default 512 tokens"
- Q2: What prevents the LLM from inventing page numbers in citations? → Expected: "[CITE:c_<8hex>] token injection + post-generation validator"
- Q3: What happens if retrieval returns 0 chunks? → Expected: "guardrail short-circuits, returns canned no-context, LLM not called"
- Q4: How does to_ragflow.py avoid re-uploading unchanged files? → Expected: "doc_id = sha256(path + content)[:32], same input → same id, idempotent"
- Q5: How many queries are in the eval set, and how many are out-of-scope? → Expected: "≥30 total; ≥3 out-of-scope (verifies guardrail)"

If all 5 are answerable from the listed docs alone (no other file access), AIH-02 passes.

## Last Touched By

Phase 5 plan 05-04 (REQ-coverage matrix + AI 接力 polish), 2026-05-03.
```

Constraints:
- File ≥ 50 lines.
- All 8 RAG REQs in the Per-REQ-ID Verification Map.
- All 6 ROADMAP SCs in the Success-Criterion Verification table.
- Verifier commands are real shell commands (executable copy-paste).
- Phase-5 Boundary Checks include the 4 design-only discipline checks above.
- 5-Minute Stranger Test section has 5 expected Q&A.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md && [ "$(wc -l < .planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md)" -ge 50 ] && for r in RAG-01 RAG-02 RAG-03 RAG-04 RAG-05 RAG-06 RAG-07 RAG-08; do grep -q "$r" .planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md || { echo "MISSING $r"; exit 1; }; done && for sc in SC-1 SC-2 SC-3 SC-4 SC-5 SC-6; do grep -q "$sc" .planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md || { echo "MISSING $sc"; exit 1; }; done && grep -q "5-Minute Stranger Test" .planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md && grep -q "Phase-5 Boundary Checks" .planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md && grep -q "stdlib-only" .planning/phases/05-rag-pipeline-design-document-only-no-run/05-COVERAGE.md</automated>
  </verify>
  <done>05-COVERAGE.md exists; ≥50 lines; all 8 RAG REQs + all 6 ROADMAP SCs in matrices; Phase-5 Boundary Checks present; 5-Minute Stranger Test with 5 Q&A; verifier commands are executable.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `wc -l .planning/design/RAG_PIPELINE.md` ≥ 500 (was ≥450 from 05-01; +§11 adds ≥50 lines)
2. `grep -c "## " .planning/design/RAG_PIPELINE.md` ≥ 11 (sections 1-11)
3. AI 接力开发指南 Locked-vs-Directional table has ≥15 Locked + ≥4 Directional rows
4. `.planning/phases/05-.../05-COVERAGE.md` exists and lists all 8 REQs + all 6 SCs
5. Regression check: `python scripts/exporters/to_ragflow.py --help` STILL works (this plan didn't touch the file)
6. Regression check: `python -c "import yaml; yaml.safe_load(open('evaluation/queries.yaml'))"` STILL parses
7. The phase verifier (orchestrator's next step) can copy-paste any single verifier command from 05-COVERAGE.md and it will execute
</verification>

<success_criteria>
All 6 ROADMAP Phase-5 success criteria pass automated grep + python checks (verifier commands in 05-COVERAGE.md).
All 8 Phase-5 REQ-IDs (RAG-01..08) traced forward (RAG_PIPELINE.md §11) AND reverse (05-COVERAGE.md).
AIH-01 satisfied: every Phase-5 design doc carries AI 接力开发指南.
AIH-02 spot check: 5-Minute Stranger Test plan documented (full execution is Phase 6 scope).
No new dependencies; design-only discipline preserved.
</success_criteria>

<output>
After completion: Create `.planning/phases/05-rag-pipeline-design-document-only-no-run/05-04-SUMMARY.md`. Record §11 line count, refreshed AI 接力 table row counts (Locked / Directional), 05-COVERAGE.md line count, and the line: "Phase 5 closed — orchestrator: run each verifier command in 05-COVERAGE.md to confirm REQ + SC closure."
</output>
