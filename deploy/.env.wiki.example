# Phase 8b local-dev env example. Copy to deploy/.env.wiki and customize.
# .env.wiki is gitignored — never commit real credentials.
WIKI_DB_USER=wikijs
WIKI_DB_PASS=<CHANGE_ME_local_dev_password>
WIKI_DB_NAME=wiki
WIKI_HOST_PORT=3000
