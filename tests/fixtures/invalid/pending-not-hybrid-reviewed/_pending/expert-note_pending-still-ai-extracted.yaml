# INVALID FIXTURE — failure mode: provenance.pending-gate (PROV-05)
# Trigger: file path contains `/_pending/` AND provenance.method != "hybrid_reviewed".
#   PROV-05 says: anything in _pending/ that hasn't been promoted via hybrid_reviewed
#   is a stuck-in-quarantine error.
# CRITICAL PATH: this file MUST live under /_pending/ subpath (NOT next to it) so the
#   path-based rule fires. plan 03-VALIDATION VAL-03 (PROV-05 path) check explicitly
#   asserts: find tests/fixtures/invalid/pending-not-hybrid-reviewed/_pending -name '*.yaml' | wc -l == 1
# Expected validator: schema PASSES; provenance.validate_record returns
#   ValidationError(rule="provenance.pending-gate", ...).
# REQ-ID: PROV-05
id: aviationkb://expert-note/pending-still-ai-extracted-demo@1
type: ExpertNote
schema_version: "0.1.0"
i18n:
  label:
    zh: "_pending 目录但未升级为 hybrid_reviewed——升级门控失败示例"
    en: "Sitting in _pending/ but still ai_extracted — promotion gate failure"
author: aviationkb://person/jane-doe@1
topic: pending-gate-demo
content_md: |
  This record was AI-extracted into _pending/ and never promoted. The validator must
  reject it for occupying the quarantine area without satisfying the hybrid_reviewed
  promotion gate (D-17). Confidence score is intentionally low (≤ 0.85) so the
  H-Darrieus rule does NOT fire here — the failure mode under test is the pending-gate
  alone, not an H-Darrieus violation.
provenance:
  method: ai_extracted
  actor: aviationkb://person/jane-doe@1
  actor_role: "ai-operator"
  created_at: "2026-05-03T00:00:00Z"
  tool: "claude-opus-4-7"
confidence:
  score: 0.50
  rationale: "Low confidence to keep H-Darrieus quiet; this fixture isolates pending-gate alone."
source:
  document_id: aviationkb://document/fixture-source-doc@1
  locator:
    page: 1
  retrieval:
    url: "https://example.invalid/fixtures/pending-gate"
    retrieved_at: "2026-05-03T00:00:00Z"
