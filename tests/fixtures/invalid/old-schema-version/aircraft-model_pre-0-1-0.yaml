# INVALID FIXTURE — failure mode: provenance.schema-version-mismatch (warning)
# Trigger: schema_version "0.0.1" is older than the current ontology version "0.1.0".
# Expected validator: emits a WARNING (not ERROR) for any version != "0.1.0" with
#   rule="provenance.schema-version-mismatch".
# NOTE: the strict-reject (older than N-1) cannot fully fire until ontology bumps to
#   v0.2.0+; in v0.1.0 the validator fires a warning instead so the path is exercised.
# REQ-ID: VER-03
id: aviationkb://aircraft-model/old-schema-version-demo@1
type: AircraftModel
schema_version: "0.0.1"
i18n:
  label:
    zh: "旧版 schema_version 示例"
    en: "AircraftModel demo with pre-0.1.0 schema_version"
manufacturer: "DemoCorp"
model_designation: "DEMO-003"
provenance:
  method: human
  actor: aviationkb://person/jane-doe@1
  actor_role: "systems-engineer"
  created_at: "2026-05-03T00:00:00Z"
confidence:
  score: 0.95
  rationale: "Older schema_version chosen to trigger the version-mismatch warning."
source:
  document_id: aviationkb://document/fixture-source-doc@1
  locator:
    page: 1
  retrieval:
    url: "https://example.invalid/fixtures/old-schema-version"
    retrieved_at: "2026-05-03T00:00:00Z"
